/**
 * Nova's standard library surface (Option/Result helpers, collection
 * utilities) available to `.nova`/`.nv` files without an explicit import.
 * See language/type-system.md for Option<T>/Result<T, E>.
 */
export {};
