/**
 * The small runtime helper library the emitter's generated code imports
 * from (state patching, task-group cancellation, RPC client). Kept
 * intentionally tiny — see "Zero Runtime Overhead" in the project vision.
 * Mirrors packages/runtime, which is the published npm package version of
 * this; this local copy is for in-repo compiler tests.
 */
export {};
