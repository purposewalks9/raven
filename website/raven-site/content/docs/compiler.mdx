---
title: Compiler Architecture
description: How Raven source becomes JavaScript, and where type intelligence lives in the compiler.
---

Raven's compiler started as pure TypeScript. As of PR #18, the type checker moved to a native Rust core (`crates/raven-core`), reached through a native Node binding — more stages are moving over as they're proven to need it (see [Roadmap](/docs/roadmap)). The goal was never "replace TypeScript"; it's to make Raven's compiler do more of the type work TypeScript asks developers to write manually, and to do it fast.

## Pipeline

Raven source moves through these stages:

1. **Lexer** — tokenizes source and records file/line/column locations for
   diagnostics. {/* TODO(phase-2): update to "native (crates/raven-core)" once merged */}
2. **Parser** — builds the AST and recognizes declarations, expressions,
   functions, imports, arrays, records, and `model` declarations.
   {/* TODO(phase-2): update to "native (crates/raven-core)" once merged */}
3. **Binder + SymbolTable** — records declarations, references, mutability,
   type, and origin metadata for names visible in a file.
4. **TypeChecker** *(native, Rust)* — infers expression types, validates assignments and calls,
   resolves models from the workspace registry, and reports diagnostics.
5. **Optimizer** — simplifies obvious dead branches and unreachable code.
   {/* TODO(phase-3): update to "native (crates/raven-core)" once merged */}
6. **Emitter** — emits JavaScript.
   {/* TODO(phase-3): update to "native (crates/raven-core)" once merged */}

## Where the compiler runs

Not every stage runs in the same language. The type checker runs as native
Rust (`crates/raven-core`) reached through a Node binding (`crates/raven-node`);
the rest of the pipeline is still TypeScript today. This is a deliberate,
staged migration for performance — see [Roadmap](/docs/roadmap) for what's
shipped and what's next, and [Performance](/docs/improvements) for what was
actually measured, not just planned. For the planned per-phase detail, see
[Phases](/docs/phases/phase-2) (Phase 2 → 5).

## Program model

The binder tracks not only what a symbol is, but where it came from:

- `local` — explicitly declared in this file
- `inferred` — created from a value with no annotation
- `import` — resolved from an import declaration
- `model` — resolved from a published model shape
- `builtin` — supplied by the compiler/runtime

That origin metadata is the foundation for better future errors like “this field
is required because it came from `model User` in `models.rv`.”

## Type engine

The type engine has two different checks:

- `sameType(a, b)` asks whether two types are exactly the same.
- `isAssignableTo(value, target)` asks whether a value can flow into an expected
  type.

That split matters. JavaScript and TypeScript code often uses compatible shapes
that are not textually identical. Raven should understand compatibility without
forcing developers to write `interface` declarations first.

## Project model

`model` declarations publish record shapes into a workspace registry shared by
all files in a project. This is how Raven removes `export type` / `import type`
for project-owned data shapes.

```lua
model user = { id: 1, name: "Ada" }
```

Every file can use `user` directly. If two files publish the same model name with
different shapes, the compiler reports a conflict with a field-level diff.
